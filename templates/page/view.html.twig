{% extends 'base.html.twig' %}

{% block title %}{{ page.title }} — {{ parent() }}{% endblock %}

{% block body %}
    <article class="blog-post">
        <header>
            <h1 class="title">{{ page.title }}</h1>
        </header>

        <section class="text">{{ page.content|raw }}</section>
    </article>
{% endblock %}